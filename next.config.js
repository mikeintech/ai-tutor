/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    emotion: true,
  },
  // devIndicators: {
  //   autoPrerender: false,
  // },
}

module.exports = nextConfig
